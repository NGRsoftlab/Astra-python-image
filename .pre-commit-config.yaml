exclude: |
  (?x)^(
      .*\{\{.*\}\}.*|     # Exclude any files with cookiecutter variables
      docs/site/.*|       # Exclude mkdocs compiled files
      \.history/.*|       # Exclude history files
      .*cache.*/.*|       # Exclude cache directories
      .*venv.*/.*|        # Exclude virtual environment directories
  )$
fail_fast: false
default_install_hook_types:
  - pre-commit
  - commit-msg
  - pre-push
  - post-rewrite
default_stages: [pre-commit]

repos:
# ---------------------------------------------------------------------------- #
#                              🔄 Pre-Commit Hooks                             #
# ---------------------------------------------------------------------------- #

# ----------------------------- 🔒 Security Tools ---------------------------- #
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        name: "🔒 security · Detect hardcoded secrets by detect-secrets"
        ## Generated by `detect-secrets scan > .secrets.baseline`
        args: ['--baseline', '.secrets.baseline']
        exclude: ^(tests|mock_data)/

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.28.0
    hooks:
      - id: gitleaks
        name: "🔒 security · Detect hardcoded secrets by gitleaks"

# --------------------------- 🔍 Code Quality Tools -------------------------- #
  ### Markdown ###
  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.18.1
    hooks:
      - id: markdownlint-cli2
        name: "📝 markdown · Format markdown via markdownlint-cli2"
        args: [--fix]

  ### Shell/Bash ###
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.10.0.1
    hooks:
      - id: shellcheck
        name: "🐚 shell · Lint shell scripts"
        require_serial: true
        exclude: ^legacy/

  - repo: https://github.com/scop/pre-commit-shfmt
    rev: v3.12.0-2
    hooks:
      - id: shfmt
        name: "🐚 shell · Format shell scripts"
        args: [
          -i=2,
          -bn,
          -ci,
          -s,
          -w
        ]

      - id: shfmt
        name: "🐚 shell · Test shell scripts format"
        args: [
          -i=2,
          -bn,
          -ci,
          -s,
          -d
        ]

  - repo: https://github.com/openstack/bashate
    rev: 2.1.1
    hooks:
      - id: bashate
        name: "🐚 shell · Check shell script code style"
        args: ['-i', 'E003,E010']

  ### Syntax ###
  - repo: https://github.com/codespell-project/codespell
    rev: v2.4.1
    hooks:
      - id: codespell
        name: "📝 codespell · Validate files for spelling errors"
        args: ['-I', '.codespell-exclude-words']
        additional_dependencies:
          - tomli

  ### Docker ###
  - repo: https://github.com/hadolint/hadolint.git
    rev: v2.13.1-beta
    hooks:
      - id: hadolint-docker
        name: "🐋 docker · Lint Dockerfile"

  ### PNG Images ###
  - repo: https://github.com/shssoichiro/oxipng
    rev: v9.1.5
    hooks:
      - id: oxipng
        name: "🖼️ images · Optimize PNG files"
        args: ["-o", "4", "--strip", "safe", "--alpha"]

# ---------------------------- 📁 Filesystem Tools --------------------------- #
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      # Filesystem Checks
      - id: trailing-whitespace
        name: "📁 filesystem/⌨️ whitespaces · Verify shebang presence"
      - id: end-of-file-fixer
        name: "📁 filesystem/⌨️ eof · Makes sure files end in a newline and only a newline"
      - id: detect-private-key
        name: "📁 filesystem/🔒 security · Checks for the existence of private keys"
      - id: check-executables-have-shebangs
        name: "📁 filesystem/⚙️ exec · Verify shebang presence"
      - id: check-shebang-scripts-are-executable
        name: "📁 filesystem/⚙️ exec · Verify script permissions"
      - id: check-case-conflict
        name: "📁 filesystem/📝 names · Check case sensitivity"
      - id: mixed-line-ending
        name: "📁 filesystem/⌨️ line ending · Replaces or checks mixed line ending"
        args: [--fix=lf]
# ------------------------------- 🌳 Git Tools ------------------------------- #
      - id: check-merge-conflict
        name: "🌳 git · Detect conflict markers"
      - id: check-added-large-files
        name: "🌳 git · Block large file commits"
        args: [--maxkb=5120]

# ---------------------------- 🐌 Selfcheck ---------------------------------- #
  - repo: meta
    hooks:
      - id: check-hooks-apply
        name: "🦟 pre-commit · Ensures that the configured hooks apply to at least one file in the repository"

# ---------------------------------------------------------------------------- #
#                            📝 Commit Message Hooks                           #
# ---------------------------------------------------------------------------- #
#
# --------------------------- ✍️ Git Commit Quality -------------------------- #
  - repo: local
    hooks:
      - id: custom-commit-parser
        name: "🌳 git · Validate commit message"
        entry: |
          bash -c '
            # MSG=$(git log -1 --pretty=%B);
            MSG_FILE="${1}";
            MSG=$(<"${MSG_FILE}");
            RE="^((Merge[ a-z-]* branch.*)|(Revert*)|((feat|fix|docs|style|refactor|perf|test|chore|ci|build|revert)(\(.*\))?!?: .{10,}))";
            if ! [[ ${MSG} =~ ${RE} ]]; then
              echo >&2 "[POLICY] Invalid commit message format!";
              echo >&2 "[ERROR]: Commit message must follow Conventional Commits";
              echo >&2 "Message must follow the pattern: type(scope): description"
              echo >&2 "Allowed types: feat, fix, docs, style, refactor, test, chore, perf, ci, build, revert";
              echo >&2 "Description must be min 10 chars";
              echo >&2 "Example: feat(auth): Add login button";
              exit 1;
            fi
          '
        args: ["COMMIT_EDITMSG"]
        language: system
        stages: [commit-msg, post-rewrite]

      - id: branch-name
        name: "🌳 git · Validate commit branch"
        entry: |
          bash -c '
            # BRANCH=$(git branch --show-current);
            BRANCH=$(git symbolic-ref --short HEAD 2>/dev/null || echo "(detached)");
            if [[ "$BRANCH" == "(detached)" ]]; then
              echo >&2 "[POLICY] Cannot push from detached HEAD!";
              exit 1;
            fi

            RE="^(feature|fix|hotfix)/.{5,}|master|main|test|dev.*$";
            if ! [[ ${BRANCH} =~ ${RE} ]]; then
              echo >&2 "[POLICY] Invalid branch name format!";
              echo >&2 "[ERROR]: Branch name must follow in specific type";
              echo >&2 "Name after branch type must be min 5 chars";
              echo >&2 "Allowed types: feature, fix, hotfix";
              echo >&2 "Examples:";
              echo >&2 "hotfix/issue-[NUM]-desc";
              echo >&2 "feature/[JIRA-ID]-short-desc";
              echo >&2 "dev*|test|master|main";
              exit 1;
            fi
          '
        language: system
        stages: [pre-push]

# ---------------------------------------------------------------------------- #
#                             🧪 Fast Tests (Local)                            #
# ---------------------------------------------------------------------------- #
  - repo: local
    hooks:
      - id: check-todos
        name: "🐚 shell · Check 'TODO/FIXME' on shell files"
        entry: |
          bash -c '! grep -rn "FIXME\|TODO" --include="*.sh" . || (echo "[ERROR]: Remove TODOs before commit!" && exit 1)'
        language: system

      - id: auto-update-pre-commit
        name: "🦟 pre-commit · Auto-update pre-commit"
        entry: bash -c 'pre-commit autoupdate || true'
        language: system
        always_run: true

      - id: check-png-size
        name: "📁 filesystem/📝 file · Check image size"
        # bash -c 'find . \( -name '*.png' -o -name '*.webp' -o -name '*.svg' -o -name '*.jpg' \) -size +500k -exec echo "[ERROR]: {} is too large" \; | grep '.' && exit 1 || true'
        entry: |
          bash -c 'git diff --cached --name-only | grep -Ei "\\.(svg|png|webp|jpe?g)\$" | while read -r f; do size=$(wc -c < "${f}"); if [[ ${size} -gt 512000 ]]; then echo "[ERROR]: ${f} is too large (${size} bytes > 500KB)" >&2; exit 1; fi; done'
        language: system
